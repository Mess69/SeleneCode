[package]
name = "selene-sync"
description = "File watcher (notify) with debounce + filter; git-hook helpers."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]

[lints]
workspace = true
