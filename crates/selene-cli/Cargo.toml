[package]
name = "selene-cli"
description = "CLI (clap), daemon, telemetry, upgrade."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
