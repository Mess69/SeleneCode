[package]
name = "selene-installer"
description = "Multi-agent installer: targets + registry + TOML/JSON/JSONC writers."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
